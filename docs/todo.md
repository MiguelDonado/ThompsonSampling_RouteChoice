- Route length and free flow travel times of routes and output to a file
  
- Approximate true hidden reward distributions using 2000 simulations instead of 500.
- Leer china article
- Regret computation
- Compare Thompson sampling with another algorithms. And even compare gamma model with another bayesian models (gaussian, log normal...)

Structure:
1. Introduce multi-armed bandit problems, thompson sampling and route choice
2. Experiments with two networks.
3. Metrics (cumulative regret, convergence speed...)