- Compare Thompson sampling with another algorithms. (UCB1)
- Plot cumulative regret, and plot fraction of each action that is picked among all the simulations


- Approximate true hidden reward distributions using 2000 simulations instead of 500.
- Leer china article
- Regret computation
- And even compare gamma model with another bayesian models (gaussian, log normal...)
- Check because it seems that Thompson sampling could be implemented with RL

Structure:
1. Introduce multi-armed bandit problems, thompson sampling and route choice
2. Experiments with two networks.
3. Metrics (cumulative regret, convergence speed...)